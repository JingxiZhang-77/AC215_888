version: "3.9"
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    ports:
      - "8000:8000"
  web:
    build:
      context: ./web
      dockerfile: Dockerfile.web
    ports:
      - "5173:5173"
    depends_on:
      - api

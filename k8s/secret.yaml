apiVersion: v1
kind: Secret
metadata:
  name: gcp-credentials
  namespace: safety-event-system
type: Opaque
data:
  # Base64 encoded GCP service account JSON key
  # To create: kubectl create secret generic gcp-credentials \
  #   --from-file=llm-service-account.json=./secrets/llm-service-account.json \
  #   -n safety-event-system
  # This file is a placeholder - use kubectl command above to create actual secret
